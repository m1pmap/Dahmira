<Window x:Class="Dahmira.Pages.FullImagePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dahmira.Pages"
        mc:Ignorable="d"
        Title="FullImagePage" Height="450" Width="800" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent" KeyDown="Window_KeyDown" MouseDown="Window_MouseDown">

    <Window.Resources>
        <ControlTemplate TargetType="Button" x:Key="btTemplate">
            <Border x:Name="border"
            CornerRadius="5"
            Background="{TemplateBinding Background}">
                <StackPanel Orientation="Horizontal" 
    HorizontalAlignment="Center" 
    VerticalAlignment="Center">
                    <ContentPresenter HorizontalAlignment="Center" 
              VerticalAlignment="Center" 
              Content="{TemplateBinding Content}" 
              ContentTemplate="{TemplateBinding ContentTemplate}"/>
                </StackPanel>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="LightBlue"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="45"/>
        </Grid.ColumnDefinitions>
        <Border Background="White" BorderThickness="1.7" BorderBrush="Gray" CornerRadius="15" Grid.Column="0">
            <Grid>
                <Image Margin="5" x:Name="fullImage" Stretch="Uniform" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Button x:Name="exit_button" Grid.Column="1"
                Template="{StaticResource btTemplate}" 
                Height="35" Width="35"
                Background="OrangeRed" 
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                Margin="5,15,0,0" 
                ToolTip="Закрыть окно"
                Click="exit_button_Click">

            <Image Source="/resources/images/delete_image.png" Height="28" Width="28" Margin="0,0,0,0"/>
        </Button>
    </Grid>

</Window>
