<Window x:Class="Dahmira.Pages.DialogPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dahmira.Pages"
        mc:Ignorable="d"
        Title="" Height="123" Width="500" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" WindowStyle="None" WindowStartupLocation="CenterScreen" BorderThickness="0" BorderBrush="LightGray">
    <Window.Resources>
        <ControlTemplate TargetType="Button" x:Key="btTemplate">
            <Border x:Name="border"
                CornerRadius="8" 
                Background="{TemplateBinding Background}">
                <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                    <ContentPresenter HorizontalAlignment="Center" 
                                  VerticalAlignment="Center" 
                                  Content="{TemplateBinding Content}" 
                                  ContentTemplate="{TemplateBinding ContentTemplate}"/>
                </StackPanel>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="LightBlue"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontFamily" Value="Arial Black"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                  Background="White"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="1.7"
                  CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Изменение цвета при фокусе -->
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="CornFlowerBlue"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White" BorderThickness="1.7" BorderBrush="Gray">
        <Grid Margin="5">
            <StackPanel>
                <Label x:Name="title" Content="Название листа Excel для экспорта:" HorizontalAlignment="Center" Margin="5,0,0,0" FontFamily="Arial Black" Width="Auto" Foreground="Gray" FontSize="18"/>
                <Label Content="Введите имя:" FontFamily="Arial" Width="Auto" Foreground="Gray" FontSize="12"/>
                <TextBox x:Name="FileName" Text="Новый лист" Margin="0,-5,5,0"
                         BorderBrush="Gray" 
                         FontFamily="Arial" 
                         Foreground="Gray" 
                         FontSize="15" Padding="2" 
                         VerticalContentAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,5" HorizontalAlignment="Right">
                    <Button x:Name="OK_button"
                            Margin="5,0"
                            Content="OК"
                            Foreground="White"
                            FontSize="14"
                            FontFamily="Arial Black"
                            Template="{StaticResource btTemplate}" 
                            Height="25" Width="80"
                            Background="MediumSeaGreen" 
                            VerticalAlignment="Top"
                            Click="OK_button_Click"/>

                    <Button x:Name="Cancel_button"
                            Content="ОТМЕНА"
                            Foreground="White"
                            Margin="5,0"
                            FontSize="14"
                            FontFamily="Arial Black"
                            Template="{StaticResource btTemplate}" 
                            Height="25" Width="120"
                            Background="OrangeRed" 
                            VerticalAlignment="Top"
                            Click="Cancel_button_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
    
</Window>
