<Window x:Class="Dahmira.Pages.WhatsNewPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dahmira.Pages"
        mc:Ignorable="d"
        Title="WhatsNewPage" Height="965" Width="855"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Closed="Window_Closed" Loaded="Window_Loaded" PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <ControlTemplate TargetType="Button" x:Key="btTemplate">
            <Border x:Name="border"
                CornerRadius="7"
                Background="{TemplateBinding Background}">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                    <ContentPresenter HorizontalAlignment="Center" 
                  VerticalAlignment="Center" 
                  Content="{TemplateBinding Content}" 
                  ContentTemplate="{TemplateBinding ContentTemplate}"/>
                </StackPanel>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="LightBlue"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!--Стиль для TabControl-->
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="0" BorderBrush="#a3a3a4" CornerRadius="4,0,0,4" Margin="-2,0,-2,10">
                            <ContentPresenter x:Name="ContentSite"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"
                                     ContentSource="Header"
                                     Margin="25,7"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter Property="FontFamily" Value="Arial Black"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#a3a3a4"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontFamily" Value="Arial Black"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="Border" CornerRadius="4"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="1.7"
            Padding="8,4">
                <Grid>
                    <Path x:Name="Arrow"
                  HorizontalAlignment="Right"
                  Margin="0,0,4,0"
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"
                  Fill="Gray"/>
                </Grid>
            </Border>
        </ControlTemplate>

        <Style x:Key="RoundedComboBox" TargetType="ComboBox">
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1.7"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="IsEditable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton
                            Name="ToggleButton"
                            Template="{StaticResource ComboBoxToggleButton}"
                            Grid.Column="2"
                            Focusable="false"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            ClickMode="Press"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0"/>

                            <ContentPresenter
                            Name="ContentSite"
                            IsHitTestVisible="False"
                            Content="{TemplateBinding SelectionBoxItem}"
                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                            Margin="8,4,28,4"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"/>

                            <Popup
                            Name="Popup"
                            Placement="Bottom"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            AllowsTransparency="True"
                            Focusable="False"
                            PopupAnimation="Slide">
                                <Grid
                            Name="DropDown"
                            SnapsToDevicePixels="True"
                            MinWidth="{TemplateBinding ActualWidth}"
                            MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border
                                Background="White"
                                BorderBrush="Gray"
                                BorderThickness="1.7"
                                CornerRadius="4">
                                        <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="CornflowerBlue"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <!-- Градиентный фон (добавлен первым, чтобы был под другими элементами) -->
        <Border CornerRadius="15" BorderThickness="1.7" BorderBrush="Gray">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <!-- Красный в начале -->
                    <GradientStop Color="White" Offset="0"/>
                    <!-- Оранжевый на 25% длины -->
                    <GradientStop Color="White" Offset="0.25"/>
                    <!-- Желтый на 50% длины -->
                    <GradientStop Color="White" Offset="0.5"/>
                    <!-- Белый в конце -->
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Основной контент (остаётся без изменений, будет поверх градиента) -->
        <Border Grid.Column="0" Background="Transparent" CornerRadius="15">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal">

                    <Label Content="Что нового?" FontFamily="Arial" Foreground="Gray" FontSize="17"/>

                    <ComboBox x:Name="updates_comboBox" Style="{StaticResource RoundedComboBox}" Background="Transparent" 
                              DisplayMemberPath="Version"
                              SelectionChanged="updates_comboBox_SelectionChanged" Width="600">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>
                    <Label x:Name="versionDate_label" Content="dd.MM.yyyy" FontFamily="Consolas" Foreground="Red" FontSize="15"/>
                </StackPanel>

                <ScrollViewer x:Name="updatesConteiner_scrollViewer" VerticalScrollBarVisibility="Hidden" Height="860">
                    <StackPanel x:Name="updates_container" Margin="0,0,15,0"/>
                </ScrollViewer>

                <CheckBox x:Name="dontShowAnymore_checkBox" Margin="0,10,0,0"
                      Content="Больше не показывать" VerticalContentAlignment="Center" 
                      FontFamily="Arial" Foreground="Gray" FontSize="15"/>
            </StackPanel>
        </Border>

        <!-- Кнопка закрытия (остаётся без изменений) -->
        <Button x:Name="exit_button" Grid.Column="1"
            Template="{StaticResource btTemplate}" 
            Height="30" Width="30"
            Background="OrangeRed" 
            VerticalAlignment="Top"
            HorizontalAlignment="Center"
            Margin="0,10,0,0" 
            ToolTip="Закрыть окно"
            Click="exit_button_Click">
            <Image Source="/resources/images/delete_image.png" Height="22" Width="22"/>
        </Button>
    </Grid>
</Window>
