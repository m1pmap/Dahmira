<Window x:Class="Dahmira.Pages.ProgressBarPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dahmira.Pages"
        mc:Ignorable="d"
        Title="ProgressBarPage" Height="54" Width="605" WindowStyle="None" Closing="Window_Closing"
        WindowStartupLocation="CenterScreen"
        BorderThickness="0"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <ControlTemplate TargetType="Button" x:Key="btTemplate">
            <Border x:Name="border"
                    CornerRadius="5"
                    Background="{TemplateBinding Background}">
                <StackPanel Orientation="Horizontal" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">
                    <ContentPresenter HorizontalAlignment="Center" 
                      VerticalAlignment="Center" 
                      Content="{TemplateBinding Content}" 
                      ContentTemplate="{TemplateBinding ContentTemplate}"/>
                </StackPanel>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="LightBlue"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="5" BorderBrush="Gray" BorderThickness="1.7">
            <StackPanel>
                <Label x:Name="progressBarLabel" Content="Загрузка" FontFamily="Arial Black" Foreground="Gray" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal">
                    <ProgressBar Margin="5,0,4,0" x:Name="progressBar" Height="20" Minimum="0" Maximum="100" Width="514" BorderThickness="0" Foreground="MediumSeaGreen" Background="LightGray"/>
                    <Button x:Name="Cancel_button"
                Template="{StaticResource btTemplate}"
                Content="ОТМЕНА"
                Height="20" Background="OrangeRed" Foreground="White" FontFamily="Arial Black"
                Click="Cancel_button_Click" Margin="0,0,10,0" Width="73"/>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</Window>
