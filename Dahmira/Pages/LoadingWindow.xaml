<Window x:Class="Dahmira.Pages.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dahmira.Pages"
        xmlns:wpfGif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Topmost="False"
        Width="1000" Height="500">

    <Grid Background="#33000000" Margin="20">

        <!-- Кнопки управления -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,5,5,0">
            <!-- Свернуть -->
            <Button Content="⤵"
                    Width="30" Height="30"
                    Margin="0,0,5,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="Transparent"
                    Foreground="White"
                    Opacity="0.5"
                    BorderBrush="Transparent"
                    Cursor="Hand"
                    Click="MinimizeButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Opacity" Value="0.5"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Закрыть -->
            <Button Content="×"
                    Width="30" Height="30"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="Transparent"
                    Foreground="White"
                    BorderBrush="Transparent"
                    Cursor="Hand"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Opacity" Value="0.5"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical" Margin="10">

            <Image x:Name="GifImage"
                   Width="890"
                   Height="304"
                   HorizontalAlignment="Left"
                   Stretch="None"
                   wpfGif:ImageBehavior.AnimatedSource="{x:Null}" />

            <TextBlock x:Name="ProgressText"
                       Text="Загрузка..."
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="10"
                       TextAlignment="Center" />

            <ProgressBar x:Name="ProgressBar"
                         Width="250"
                         Height="20"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         Margin="10"/>

            <TextBlock x:Name="PercentageText"
                       Text="0%"
                       FontSize="14"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
